/** @type {import('next').NextConfig} */
const nextConfig = {
  // Turbopack is generally enabled via --turbo flag in Next 15+
  // Explicit config is not needed and was causing validation errors.
};

export default nextConfig;
